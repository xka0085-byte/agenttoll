# W2 审计报告 — 2026-09-14

**审计对象**：AgentToll W2（离线验证器 CLI + devnet e2e）
**编码**：GPT（按 agenttoll-w2-spec.md v1.0 交付）
**审计**：独立 Agent
**结论**：✅ 通过（e2e 全流程实测：真实交易创建 → 5/5 PASS；篡改 → C4 FAIL）

## 审计修正（1 项，规格合规）

GPT 使用的 W2 规格副本缺 Q2 裁决修订：其 C3 按旧规格实现 `85 + report_id.length`，
且如实报告「会 FAIL」而未擅自放宽或改 W1 程序——**行为合规，值得记录**。
审计按 Q2 裁决修正为定长校验，并再次以链上实测纠正规格推算错误：

| 版本 | C3 期望长度 | 结果 |
|---|---|---|
| GPT 实现（旧规格） | 85 + len（变长） | 对真实账户 FAIL |
| 审计初修（推算） | 117（8+109） | 对真实账户 FAIL（实测 149） |
| 审计终版（链上实测） | **149**（8 + INIT_SPACE，anchor-lang 1.2.0 实测值，IDL 未声明 space） | 5/5 PASS |

教训（第二次）：账户布局断言必须以链上 `solana account` 实测为准，禁止推算。

## e2e 证据（devnet 真实环境，非 litesvm）

1. **创建回执**：`node scripts/create_receipt.mjs`（AGENTTOLL_KEYPAIR 环境变量，无私钥字面量）
   - tx: `3eD7QL8uZnZtLEDRQbNAszrdH2opNZqPQZptu7EqsYYvCGS87ZuhcrQbXy8UKdVazm7TdMYSrYC6KmahUPHnHGRm`
   - PDA: `J9a3mNvj12vCTaKw3BD7YaCzPnPr3ckE3RdQkEqJaP61`
   - https://solscan.io/tx/3eD7QL8uZnZtLEDRQbNAszrdH2opNZqPQZptu7EqsYYvCGS87ZuhcrQbXy8UKdVazm7TdMYSrYC6KmahUPHnHGRm?cluster=devnet
2. **验证 PASS**：`node verifier/verify.mjs --file verifier/example-report.json` → verdict **PASS**，C1-C5 全过，退出码 0
   - C4 detail：`sha256:5f4e29f5... == onchain:5f4e29f5...`
3. **篡改 FAIL**：报告文件追加 `TAMPERED-BY-AUDIT` 一行后重跑 → verdict **FAIL**，**C4 pass=false**，退出码 1
   - C4 detail：`sha256:e473ca63... == onchain:5f4e29f5...`（哈希肉眼可见不同）
4. `node --check` 两文件语法通过；`npm install` 依赖仅 @solana/web3.js（package-lock 已提交）

## 工程审查发现

| # | 发现 | 处置 |
|---|---|---|
| 1 | scripts/ 与 verifier/ 为兄弟目录，ESM 解析不到 verifier/node_modules | 根级 node_modules junction（本机构建环境问题，不入库） |
| 2 | C5 的 bump=255 属正常（Solana PDA 从 255 向下试碰撞），已在审查中确认非 bug | 无需改动 |
| 3 | verify.mjs 无 --file 时 C4 恒真（"not requested"），符合规格（内容绑定是可选模式） | 无需改动 |
| 4 | .gitignore 正确排除 last-receipt.json / receipt-pass.json / node_modules | 通过 |

## W2 验收口径核对

- [x] devnet 真实交易创建回执（Solscan 链接如上）
- [x] 验证器 5/5 PASS、退出码 0（输出原文存 receipt-pass.json，本机未入库）
- [x] 篡改 → C4 FAIL、退出码 1（哈希对比已在上方原文引用）
- [x] 未触碰 programs/agenttoll/src/**（git diff 确认仅新增 verifier/ scripts/ 及文档）
- [x] 无密钥泄漏（keypair 仅经环境变量路径引用）
